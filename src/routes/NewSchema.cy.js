import NewSchema from './NewSchema.svelte'

describe('Stepper', () => {
    it('mounts', () => {
        cy.mount(NewSchema)
    })
    it('checks output after encode and decode', ()=>{
        
    })
})